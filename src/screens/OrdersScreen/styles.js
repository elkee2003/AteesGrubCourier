import { StyleSheet, Text, View } from 'react-native'

const styles = StyleSheet.create({
    header:{
        alignItems:'center',
        marginBottom:30,
    },
    onlineTxt:{
        fontWeight:'bold',
        fontSize:20,
        letterSpacing:0.5,
        paddingBottom:5
    },
    availableorders:{
        letterSpacing:0.5,
        color:'grey',
    },
})

export default styles